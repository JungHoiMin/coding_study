import sys

def dp(length):
    if length == x:
        return 1
    if length > x:
        return 0
    if dp[length] != -1:
        return dp[length]
    dp[length] = 0
    for i in range(N):
        for i in range():
            pass


length = []
cnt = []

N, x = map(int, sys.stdin.readline().split())
for i in range(N):
    L, C = map(int, sys.stdin.readline().split())
    length.append(L)
    cnt.append(C)

